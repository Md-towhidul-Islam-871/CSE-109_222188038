// Online C compiler to run C program online
#include <stdio.h>

void main(){
    int a, sum=0;
    
    for(a=1; a<=10; a++){
        sum+=a;
        printf("%d ", a);
    }
    printf("\nSum is: %d", sum);
}
