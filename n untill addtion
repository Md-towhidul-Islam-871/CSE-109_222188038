// Online C compiler to run C program online
#include <stdio.h>

void main(){
    int count, number, sum=0;
    printf("Enter untill number:\n");
    scanf("%d", &number);
    
    for(count=0; count<=number; count++){
        sum+=count;
    }
    printf("Adition result is: %d", sum);
}
